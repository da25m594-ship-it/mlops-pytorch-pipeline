apiVersion: v1
kind: Service

metadata:
  name: mlops-pytorch-serve

spec:
  type: NodePort

  selector:
    app: mlops-pytorch-serve

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
